# V0.9 Supabase 迁移与读取验收记录

## 1. V0.9-A 数据迁移状态

- Supabase 6 张表已创建成功
- villages 已导入 24 条
- village_profiles 已导入 24 条
- village_designations / village_routes / village_foods / village_stays 数量正常
- 迁移脚本支持重复执行
- SQL Editor 手动校验数量正确

## 2. V0.9-B Supabase 优先读取状态

- 已新增 Supabase 读取模块
- /api/plan 已改为 Supabase 优先读取
- Supabase 读取失败时自动 fallback 到本地 REAL_VILLAGES
- /api/plan 返回结构保持不变
- 前端 H5 / weapp 不需要修改

## 3. 本次问题修正

- 初始 Supabase 访问失败不是代码问题
- 原因是 SUPABASE_URL 写错
- 错误域名为 gmisnwoaigoefwwwawun.supabase.co
- 正确项目基础 URL 为 https://gmjsnwoaigcofuwyawun.supabase.co
- .env.local 中 SUPABASE_URL 必须只写项目基础 URL，不带 /rest/v1/

## 4. 已验证结果

- Node 直接请求 Supabase REST 接口成功
- 返回 status 200
- 成功读取 villages 表 village_code
- 后端 npm run dev 后，/api/plan 请求返回 200
- 后端不再出现 Supabase 读取失败 fallback 日志
- 首页 -> AI -> Result 主链路正常

## 5. 当前可用状态

- 页面功能正常
- Supabase 优先读取已通过
- 本地 REAL_VILLAGES fallback 保留
- 我的行程、换一个、查路线、觅美食、寻住处仍保持可用

## 6. 下一阶段 V1.0 建议

- 后端部署
- 配置 HTTPS
- 小程序合法域名
- 正式 AppID
- 隐私协议
- 发布前检查
- 云端环境验证 Supabase 读取
- 不再依赖本地 172.20.10.4 地址
